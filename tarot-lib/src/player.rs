// absolute
    // name: str
    // score
// phaseData:
    // playing
        // role: leader, follower, opponent
        // deck: vec<card>[]
        // heap: vec<card>[] -> cards won

pub struct Player {
    pub pk: i32,
    pub username: String,
}
